name: flutter_example
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
  sdk: ^3.1.1

dependencies:
  flutter:
    sdk: flutter

  flutter_blue_plus: ^1.34.5

  english_words: ^4.0.0
  provider: ^6.0.0
  csv: ^6.0.0
  collection: ^1.18.0
  fl_chart: ^0.69.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets: [assets/data/240704_1214_standing_old-board.csv]
